{
  "last_updated": "2026-06-23T08:26:44Z",
  "provider_version": "7.37.0",
  "arguments": {
    "action": {
      "description": "The Action to perform when the client connection triggers the rule. Valid actions are \"allow\", \"deny\", \"goto_next\" and \"apply_security_profile_group\".",
      "required": true,
      "type": "string",
      "security_impact": true,
      "rationale": "The action determines whether matching traffic is allowed through, get blocked, transferred to the next rule,or routed through a security profile for deeper inspection"
    },
    "deletion_policy": {
      "description": "Whether Terraform will be prevented from destroying the instance. Defaults to \"DELETE\".\nWhen a 'terraform destroy' or 'terraform apply' would delete the instance,\nthe command will fail if this field is set to \"PREVENT\" in Terraform state.\nWhen set to \"ABANDON\", the command will remove the resource from Terraform\nmanagement without updating or deleting the resource in the API.\nWhen set to \"DELETE\", deleting the resource is allowed.\n",
      "required": false,
      "type": "string",
      "security_impact": false,
      "rationale": "Is operational lifecycle management, not a core security control, and typically defaults to false/abandon"
    },
    "description": {
      "description": "An optional description for this resource.",
      "required": false,
      "type": "string",
      "security_impact": false,
      "rationale": "A description can help identify the purpose and scope of the firewall rule, making it easier to manage and maintain."
    },
    "direction": {
      "description": "The direction in which this rule applies. Possible values: [\"INGRESS\", \"EGRESS\"]",
      "required": true,
      "type": "string",
      "security_impact": false,
      "rationale": "The direction determines whether the rule applies to incoming or outgoing traffic."
    },
    "disabled": {
      "description": "Denotes whether the firewall policy rule is disabled.\nWhen set to true, the firewall policy rule is not enforced and traffic behaves as if it did not exist.\nIf this is unspecified, the firewall policy rule will be enabled.",
      "required": false,
      "type": "bool",
      "security_impact": true,
      "rationale": " Can silently turn off enforcement without deleting the rule, effectively exposing the network if flipped by mistake"
    },
    "enable_logging": {
      "description": "Denotes whether to enable logging for a particular rule.\nIf logging is enabled, logs will be exported to the configured export destination in Stackdriver.\nLogs may be exported to BigQuery or Pub/Sub.\nNote: you cannot enable logging on \"goto_next\" rules.",
      "required": false,
      "type": "bool",
      "security_impact": true,
      "rationale": "Enabling logging allows for the monitoring and auditing of network traffic that matches this rule."
    },
    "firewall_policy": {
      "description": "The firewall policy of the resource.",
      "required": true,
      "type": "string",
      "security_impact": false,
      "rationale": "This points to the parent policy"
    },
    "priority": {
      "description": "An integer indicating the priority of a rule in the list.\nThe priority must be a positive value between 0 and 2147483647.\nRules are evaluated from highest to lowest priority where 0 is the highest priority and 2147483647 is the lowest prority.",
      "required": true,
      "type": "number",
      "security_impact": false,
      "rationale": "An architectural/operational rule-management choice specific to the team, not a security-enforced platform target"
    },
    "project": {
      "description": "The ID of the project in which the resource belongs. If it is not provided, the provider project is used.",
      "required": false,
      "type": "string",
      "security_impact": false,
      "rationale": "Shows which GCP project owns the resource"
    },
    "rule_name": {
      "description": "An optional name for the rule. This field is not a unique identifier and can be updated.",
      "required": false,
      "type": "string",
      "security_impact": false,
      "rationale": " A label for the rule. It has no effect on how the rule works."
    },
    "security_profile_group": {
      "description": "A fully-qualified URL of a SecurityProfile resource instance.\nExample: https://networksecurity.googleapis.com/v1/projects/{project}/locations/{location}/securityProfileGroups/my-security-profile-group\nMust be specified if action = 'apply_security_profile_group' and cannot be specified for other actions.",
      "required": false,
      "type": "string",
      "security_impact": true,
      "rationale": "Picks which security profile checks the traffic. A wrong or weak profile means the check does not really work."
    },
    "target_forwarding_rules": {
      "description": "A list of forwarding rules to which this rule applies.\nThis field allows you to control which load balancers get this rule.\nFor example, the following are valid values:\n- https://www.googleapis.com/compute/v1/projects/project/global/forwardingRules/forwardingRule\n- https://www.googleapis.com/compute/v1/projects/project/regions/region/forwardingRules/forwardingRule\n- projects/project/global/forwardingRules/forwardingRule\n- projects/project/regions/region/forwardingRules/forwardingRule",
      "required": false,
      "type": "list(string)",
      "security_impact": false,
      "rationale": "References to specific load balancers/forwarding rules are resource references"
    },
    "target_service_accounts": {
      "description": "A list of service accounts indicating the sets of instances that are applied with this rule.",
      "required": false,
      "type": "list(string)",
      "security_impact": false,
      "rationale": "References to specific team service accounts, an architectural choice"
    },
    "target_type": {
      "description": "Target types of the firewall policy rule.\nDefault value is INSTANCES.\nWhen target_type is INTERNAL_MANAGED_LB, target_forwarding_rules must be set Possible values: [\"INSTANCES\", \"INTERNAL_MANAGED_LB\"]",
      "required": false,
      "type": "string",
      "security_impact": false,
      "rationale": "Sets if the rule applies to instances"
    },
    "tls_inspect": {
      "description": "Boolean flag indicating if the traffic should be TLS decrypted.\nCan be set only if action = 'apply_security_profile_group' and cannot be set for other actions.",
      "required": false,
      "type": "bool",
      "security_impact": true,
      "rationale": "Decides if traffic gets decrypted for checking. Without this, hidden threats in encrypted traffic can pass through"
    },
    "match": {
      "type": "block",
      "description": "A match condition that incoming traffic is evaluated against. If it evaluates to true, the corresponding 'action' is enforced.",
      "required": true
    },
    "match.dest_address_groups": {
      "description": "Address groups which should be matched against the traffic destination. Maximum number of destination address groups is 10.",
      "required": false,
      "type": "list(string)",
      "security_impact": false,
      "rationale": "Sets which address groups count as the destination"
    },
    "match.dest_fqdns": {
      "description": "Fully Qualified Domain Name (FQDN) which should be matched against traffic destination. Maximum number of destination fqdn allowed is 100.",
      "required": false,
      "type": "list(string)",
      "security_impact": false,
      "rationale": "Team-managed routing/traffic choices"
    },
    "match.dest_ip_ranges": {
      "description": "CIDR IP address range. Maximum number of destination CIDR IP ranges allowed is 5000.",
      "required": false,
      "type": "list(string)",
      "security_impact": false,
      "rationale": "Sets which IP ranges count as the destination"
    },
    "match.dest_network_context": {
      "description": "Network context of the traffic destination. Possible values: [\"UNSPECIFIED\", \"INTERNET\", \"INTRA_VPC\", \"NON_INTERNET\", \"VPC_NETWORKS\"]",
      "required": false,
      "type": "string",
      "security_impact": false,
      "rationale": "Network context is an architectural routing distinction"
    },
    "match.dest_region_codes": {
      "description": "Region codes whose IP addresses will be used to match for destination of traffic. Should be specified as 2 letter country code defined as per ISO 3166 alpha-2 country codes. ex.\"US\" Maximum number of dest region codes allowed is 5000.",
      "required": false,
      "type": "list(string)",
      "security_impact": true,
      "rationale": "Sets which countries count as the destination. Used to block or allow traffic by region."
    },
    "match.dest_threat_intelligences": {
      "description": "Names of Network Threat Intelligence lists. The IPs in these lists will be matched against traffic destination.",
      "required": false,
      "type": "list(string)",
      "security_impact": true,
      "rationale": "Sets which threat lists to check the destination against. Decides which known bad destinations get caught. A wrong list can let traffic through to known bad destinations or block good traffic."
    },
    "match.src_address_groups": {
      "description": "Address groups which should be matched against the traffic source. Maximum number of source address groups is 10.",
      "required": false,
      "type": "list(string)",
      "security_impact": false,
      "rationale": "Sets which address groups count as the source"
    },
    "match.src_fqdns": {
      "description": "Fully Qualified Domain Name (FQDN) which should be matched against traffic source. Maximum number of source fqdn allowed is 100.",
      "required": false,
      "type": "list(string)",
      "security_impact": false,
      "rationale": "Sets which domain names count as the source"
    },
    "match.src_ip_ranges": {
      "description": "CIDR IP address range. Maximum number of source CIDR IP ranges allowed is 5000.",
      "required": false,
      "type": "list(string)",
      "security_impact": false,
      "rationale": "Defines which source IP ranges the rule matches"
    },
    "match.src_network_context": {
      "description": "Network context of the traffic source. Possible values: [\"UNSPECIFIED\", \"INTERNET\", \"INTRA_VPC\", \"NON_INTERNET\", \"VPC_NETWORKS\"]",
      "required": false,
      "type": "string",
      "security_impact": false,
      "rationale": "An architectural routing distinction"
    },
    "match.src_networks": {
      "description": "Networks of the traffic source. It can be either a full or partial url.",
      "required": false,
      "type": "list(string)",
      "security_impact": false,
      "rationale": "References to specific team networks, resource pointers"
    },
    "match.src_region_codes": {
      "description": "Region codes whose IP addresses will be used to match for source of traffic. Should be specified as 2 letter country code defined as per ISO 3166 alpha-2 country codes. ex.\"US\" Maximum number of source region codes allowed is 5000.",
      "required": false,
      "type": "list(string)",
      "security_impact": true,
      "rationale": "Country codes matched against source, used for geo-based access control."
    },
    "match.src_threat_intelligences": {
      "description": "Names of Network Threat Intelligence lists. The IPs in these lists will be matched against traffic source.",
      "required": false,
      "type": "list(string)",
      "security_impact": true,
      "rationale": "Named threat intel lists matched against source, determines which known-malicious sources are caught."
    },
    "match.layer4_configs": {
      "type": "block",
      "description": "Pairs of IP protocols and ports that the rule should match.",
      "required": true
    },
    "match.layer4_configs.ip_protocol": {
      "description": "The IP protocol to which this rule applies. The protocol type is required when creating a firewall rule.\nThis value can either be one of the following well known protocol strings (tcp, udp, icmp, esp, ah, ipip, sctp), or the IP protocol number.",
      "required": true,
      "type": "string",
      "security_impact": false,
      "rationale": " A functional traffic configuration choice specific to the application"
    },
    "match.layer4_configs.ports": {
      "description": "An optional list of ports to which this rule applies. This field is only applicable for UDP or TCP protocol. Each entry must be either an integer or a range. If not specified, this rule applies to connections through any port.\nExample inputs include: [\"22\"], [\"80\",\"443\"], and [\"12345-12349\"].",
      "required": false,
      "type": "list(string)",
      "security_impact": false,
      "rationale": "A functional traffic configuration choice specific to the application"
    },
    "match.src_secure_tags": {
      "type": "block",
      "description": "List of secure tag values, which should be matched at the source of the traffic. For INGRESS rule, if all the srcSecureTag are INEFFECTIVE, and there is no srcIpRange, this rule will be ignored. Maximum number of source tag values allowed is 256.",
      "required": false
    },
    "match.src_secure_tags.name": {
      "description": "Name of the secure tag, created with TagManager's TagValue API.",
      "required": false,
      "type": "string",
      "security_impact": false,
      "rationale": "Team-specific resource references and labels"
    },
    "target_secure_tags": {
      "type": "block",
      "description": "A list of secure tags that controls which instances the firewall rule applies to.\nIf targetSecureTag are specified, then the firewall rule applies only to instances in the VPC network that have one of those EFFECTIVE secure tags, if all the targetSecureTag are in INEFFECTIVE state, then this rule will be ignored.\ntargetSecureTag may not be set at the same time as targetServiceAccounts. If neither targetServiceAccounts nor targetSecureTag are specified, the firewall rule applies to all instances on the specified network. Maximum number of target label tags allowed is 256.",
      "required": false
    },
    "target_secure_tags.name": {
      "description": "Name of the secure tag, created with TagManager's TagValue API.",
      "required": false,
      "type": "string",
      "security_impact": false,
      "rationale": "Team-specific resource references, not a platform policy targe"  }
  }
}
